package Product;

public class Hamburger extends Product{


    public Hamburger(int id, String name, String category, int cost, int kcal, boolean isOption, int discount, boolean isDiscount) {
        super(id, name, category, cost, kcal, isOption, discount, isDiscount);
    }
}
