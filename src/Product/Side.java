package Product;

public class Side extends Product{
    public Side(int id, String name, String category, int cost, int kcal, boolean isOption, int discount, boolean isDiscount) {
        super(id, name, category, cost, kcal, isOption, discount, isDiscount);
    }
}
